<?xml version="1.0" encoding="UTF-8"?>
<configuration  scan="true" scanPeriod="60 seconds" debug="false">
    <contextName>logback</contextName>
    <property name="log.path" value="E:\\test\\logback.log" />
    <!--输出到控制台-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
       <!-- <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>-->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %contextName [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--输出到文件-->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logback.%d{yyyy-MM-dd}.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %contextName [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="console" />
        <appender-ref ref="file" />
    </root>

	<!-- 测试环境+开发环境. 多个使用逗号隔开. -->
	<springProfile name="test,dev">
    	<logger name="corn.jamboy.contorller" level="INFO" />
    	<logger name="org.apache.tomcat" level="INFO"/>
    	<logger name="org.hibernate" level="INFO"/>
    	<logger name="org.springboot" level="DEBUG" />
    	<logger name="org.apache.tomcat.jdbc.pool" level="DEBUG"/>
	</springProfile>
	<!-- 生产环境. -->
	<springProfile name="prod">
		<logger name="corn.jamboy.contorller" level="ERROR" />
		<logger name="org.hibernate" level="WARN"/>
		<logger name="org.springboot" level="WARN" />
		<logger name="org.apache.tomcat.jdbc.pool" level="WARN"/>
	</springProfile>


    <!-- logback为java中的包 -->
    <logger name="corn.jamboy.contorller"/>
    <!--logback.LogbackDemo：类的全路径 -->
    <logger name="corn.jamboy.contorller" level="WARN" additivity="false">
        <appender-ref ref="console"/>
    </logger>
    

    <logger name="AreaInfo" level="trace" />
    <logger name="WhitelistIp" level="trace" />
    <logger name="WhitelistIpGroup" level="trace" />
    
</configuration>